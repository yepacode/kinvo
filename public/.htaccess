# Feedback auditoría seguridad 21-sep MED-3: Hostinger/LiteSpeed sobrescribe
# la CSP que envía SecurityHeaders con la suya (sólo `upgrade-insecure-requests`).
# Con `always unset` limpiamos la que agrega el server, y dejamos que la de
# PHP pase; si algún día el hosting no la quita, no hay riesgo — sobre-declarar
# doble está bien. También quitamos X-Powered-By por si expose_php no está OFF.
<IfModule mod_headers.c>
    Header always unset Content-Security-Policy
    Header always unset X-Powered-By
</IfModule>

<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
